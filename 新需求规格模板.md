# 新需求规格模板（OMS FormatScript）

> 用途：写新的业务规则 / 新脚本时填这份，交给 AI 直接生成 `@step` 代码。
>
> 设计原则：**凡是不填就会让 AI 自己编的地方，都做成必填格子。**
> 空着不填 ≠ 无所谓，AI 会给你编一个看起来很合理的行为。
>
> 配套：`Format_Center_2_脚本逻辑规格说明.md`（既有 33 步的写法参考）、
> `FC2_逐步移植.html`（可交互对照）

---

# 第一部分：空白模板

复制下面整段填写。带 ★ 的是最容易漏、也最值得花时间的。

---

## 0. 元信息

| 项 | 内容 |
|---|---|
| 需求名称 | |
| 需求来源 / 提出人 | |
| 日期 | |
| 影响的 format | 如 `FMTEV250820266189` |
| 类型 | ☐ 新增 step ☐ 改既有 step ☐ 只改 schema ☐ 只改常量/配置 |
| 关联既有 step | 如「插在 #4 和 #5 之间」/「改 #18」 |

## 1. 先确认：能不能不写 Python？

逐条打勾。**任何一条勾上，就先走那条路，别写代码。**

- ☐ 这是 composition 校验（部件/纤维/百分比的通用规则）→ schema 的 Composition Repeater + validation boolean
- ☐ 这是「某字段必填」→ schema 的 `required`
- ☐ 这是「某字段只能选这几个值」→ schema 的 `formatListId` / `enum`
- ☐ 这只是加/删一种语言 → 改语言表常量
- ☐ 这只是改文案 → 改文案常量或配置文件
- ☐ 这只是加一个直接从表里取值的输出 → 可能只要 schema 加字段 + 一个查表 step

**都不是** → 继续往下填。

## 2. 做什么

一句话。不要写实现，写目的。

```
（例：当部件的再生纤维占比超过阈值时，印上供应商的 GRS 认证编号。）
```

## 3. 输入 ★

**字段名必须和 `schema.json` 完全一致**，包括大小写。这是唯一真相 —— 不要凭印象写。

| 字段 | 来自 | 形状 / 类型 | 空值或缺失时怎么办 |
|---|---|---|---|
| | ☐ schema 输入根 ☐ 上游 step 产出 ☐ context | | |
| | | | |

> 提醒：本 format 的 native 字段名大小写是混的 ——
> `com` 用小写 `com_pt/com_pc/com_fc`；bicomponent 用大写 `Com_2_pc1`；
> 羽绒用大写 `DownCom_pt/DownCom_pc/DownCom_fc`。

## 4. 输出 ★

| 字段名（schema 确切名） | 类型 | 什么情况下为空 |
|---|---|---|
| | | |

- ☐ 这些字段已在 `outputConfig.fields` 里 → 列出确切名字
- ☐ 需要新增到 schema → **先走 schema 变更，本需求依赖它**

> 引擎会**拒绝未声明的 output key**。`Decimal` 必须先转 `str()` / `int()`。

## 5. 依赖与执行顺序 ★

| 项 | 内容 |
|---|---|
| 我要读谁的产出 | 如「#4 的 `is_rep__seq_005_018`」 |
| 谁会读我的产出 | 如「#18 翻译要用」/「没有，直接进 outputs」 |
| 必须排在谁之后 | 说明原因（读的是它改写后的值？） |
| EDI 路径要不要跑 | ☐ 要 ☐ 不要，透传 |

> 注意：`p` 只是 tie-breaker，真正排序靠 `fields`/`shared_fields` 的拓扑排序。
> **想让 A 在 B 之前，必须显式声明 handoff**，不能靠 `p` 的大小。
> 另：form-only 的 step 也要声明两个 phase，否则 EDI 路径的消费者找不到生产者。

## 6. 规则

编号，一条一句，不要写成段落。

```
R1.
R2.
R3.
```

## 7. 例子 ★★★ 最重要

**至少 4 个，缺一不可。** 散文描述留下的歧义只有例子能钉死。

### ① 正常情况
```
输入：
输出：
为什么：
```

### ② 边界（空值 / 极值 / 长度上限 / 只有一个元素）
```
输入：
输出：
为什么：
```

### ③ 反例（看起来会触发但实际不该触发）
```
输入：
输出：
为什么不触发：
```

### ④ 异常（查不到 / 转换失败 / 数据不合法）
```
输入：
输出：
为什么：
```

> 为什么强调反例：既有代码里「翻译串拼装」这条规则，散文描述听起来完备，
> 实际有三个变体（Deco 繁简都用 `|`、COO 普通语言前面不加 `/`、Fiber 收尾只掐头不去尾）。
> **散文区分不了，例子可以。**

## 8. 警告与错误文案 ★

| 触发条件 | 文案（逐字，含标点） | 挂在哪个 native 路径 | 级别 |
|---|---|---|---|
| | | `com[0].materials[2].com_fc` | ☐ warning ☐ 该 SKU 失败 ☐ 整个 Item 终止 |

- 路径用 **0 起始**的 native 写法，不是 `com1_fc3`（UI 靠它高亮定位）
- 文案有拼写错误或奇怪标点时**照抄**，并在旁边注明「原文如此」
- 默认全部用 `StepWarning`（不阻塞）；要用 error 必须写明理由

## 9. 明确不做什么 ★

容易被 AI 顺手加上的东西，在这里划掉。

```
- 不校验 …（平台管）
- 不修 …（照搬现状，见缺陷台账 X 条）
- 不改 …
```

## 10. 数据库（如果要查表）

| 项 | 内容 |
|---|---|
| 表名 | 标识符**要加引号**：`"TH_NEW3_PART"`（Postgres 会折叠未加引号的） |
| 查询 key | |
| 用到的列 | 逐个列出，别写 `select *` |
| 查不到怎么办 | ☐ 空串 ☐ warning ☐ 跳过 |
| 能否批量预取 | ☐ 能 → 放 `setup()`，整个 Item 查一次 ☐ 不能 → 说明原因 |

> schema 前缀（`public.`）目前**未确认**，暂时不加。

## 11. 待确认

列出你也不确定的地方。**写出来比留空好** —— AI 会针对它提问，而不是自己编。

```
- 
```

---

## 交给 AI 时，在最后附上这段固定要求

```
生成时请遵守：

1. 字段名以 schema.json 为唯一真相，不要参考其他文档的写法
2. 只用 if / for / while 和基本字符串方法，不用推导式、lambda、f-string
3. 不引入任何第三方库
4. 不要"顺手修复"任何看起来像 bug 的地方，照规格实现
5. 未在规格中定义的行为，不要自行决定

另外单独输出一份《我做了哪些假设》清单：
凡是规格没写死、你自己决定的，逐条列出来。
```

第 5 条和最后那段是配套的 —— 它把「AI 悄悄编了个边界行为」变成「AI 明说它编了什么」，
成本几乎为零。

---
---

# 第二部分：填好的范例

下面是一个完整填好的例子，照着这个密度写就够了。

---

## 0. 元信息

| 项 | 内容 |
|---|---|
| 需求名称 | 再生成分 GRS 认证编号印制 |
| 需求来源 | 合规部 / 张三 |
| 日期 | 2026-09-09 |
| 影响的 format | FMTEV250820266189 |
| 类型 | ☑ 新增 step |
| 关联既有 step | 插在 #4（再生纤维判断）之后、#18（部件翻译）之前 |

## 1. 先确认：能不能不写 Python？

- ☐ composition 校验 —— 不是，这是查表取编号
- ☐ 必填 —— 不是
- ☐ 值域限制 —— 不是
- ☐ 加减语言 —— 不是
- ☐ 改文案 —— 不是
- ☐ 直接从表取值的输出 —— **接近，但不是**：需要先算「哪些部件超阈值」才知道要不要取，有条件判断

→ 需要写 step。

## 2. 做什么

当某个部件的再生纤维**合计占比 ≥ 50%** 时，查出该供应商的 GRS 认证编号，
印在标签上；查不到编号要提示，但不阻塞。

## 3. 输入

| 字段 | 来自 | 形状 / 类型 | 空值或缺失时怎么办 |
|---|---|---|---|
| `com` | schema 输入根 | repeater，最多 10 个部件，每个含 `com_pt` + `materials[10]{com_pc, com_fc}` | 空数组 → 输出空串，无 warning |
| `com__seq_002` | 上游 #2 产出 | 同上（EDI 拆分后的版本） | 不会缺 |
| `vendor` | schema 输入根 | str，供应商代码 | 空 → 输出空串 + warning |

> 用 `com__seq_002` 不用原始 `com` —— 要读 EDI 拆分后的结果。

## 4. 输出

| 字段名 | 类型 | 什么情况下为空 |
|---|---|---|
| `grs_cert` | str | 没有任何部件超阈值时；或 `vendor` 为空 |

☐ 已在 `outputConfig.fields` → **☑ 需要新增，本需求依赖 schema 变更先落地**

## 5. 依赖与执行顺序

| 项 | 内容 |
|---|---|
| 我要读谁的产出 | `#2` 的 `com__seq_002` |
| 谁会读我的产出 | 没有，`grs_cert` 直接进 outputs |
| 必须排在谁之后 | `#2` —— 要读 EDI 拆分后的纤维，不是原始输入 |
| EDI 路径要不要跑 | ☑ 要（两条路径行为一致） |

## 6. 规则

```
R1. 对每个部件，把纤维名以 "reprocessed" 开头的那些的 com_pc 相加。
R2. 合计 ≥ 50 的部件，视为「超阈值部件」。
R3. 一个都没有 → grs_cert 输出空串，不查表，不 warning。
R4. 至少有一个 → 用 vendor 查 GRS 表取编号。
R5. 查到 → grs_cert = 编号。
R6. 查不到 → grs_cert 空串 + warning，挂在第一个超阈值部件上。
R7. 阈值 50 做成常量 GRS_THRESHOLD，方便后续调整。
```

## 7. 例子

### ① 正常
```
输入：
  vendor = 'V001'
  com[0] = {com_pt:'BODY', materials:[
      {com_fc:'reprocessed polyester (recycled)', com_pc:60},
      {com_fc:'cotton', com_pc:40}]}
  GRS 表：V001 → 'GRS-2026-0451'

输出：
  grs_cert = 'GRS-2026-0451'

为什么：再生纤维合计 60 ≥ 50 → 超阈值 → 查到编号
```

### ② 边界：刚好等于阈值 + 多个再生纤维相加
```
输入：
  com[0].materials = [
      {com_fc:'reprocessed polyester (recycled)', com_pc:30},
      {com_fc:'reprocessed cotton (recycled)',    com_pc:20},
      {com_fc:'silk', com_pc:50}]

输出：
  grs_cert = 'GRS-2026-0451'

为什么：30+20=50，规则是 ≥ 不是 >，所以算超阈值
```

### ③ 反例：有再生纤维但没到阈值
```
输入：
  com[0].materials = [
      {com_fc:'reprocessed polyester (recycled)', com_pc:30},
      {com_fc:'cotton', com_pc:70}]

输出：
  grs_cert = ''      无 warning

为什么不触发：30 < 50。注意这里 isRepComp 可能是 'NO'（因为部件里没有普通版
polyester 与之配对），但本规则和 isRepComp 无关，只看百分比 —— 两者是独立判断。
```

### ④ 异常：超阈值但查不到编号
```
输入：
  vendor = 'V999'（GRS 表里没有）
  com[0] 再生合计 60

输出：
  grs_cert = ''
  warning: 'GRS certificate not found for vendor V999.'
           挂在 com[0].materials[0].com_fc

为什么：不阻塞该 SKU，只提示合规部补录
```

## 8. 警告与错误文案

| 触发条件 | 文案（逐字） | native 路径 | 级别 |
|---|---|---|---|
| 超阈值但 vendor 为空 | `Vendor is required for GRS certification.` | `vendor` | ☑ warning |
| 超阈值但查不到编号 | `GRS certificate not found for vendor {vendor}.` | 第一个超阈值部件的 `com[i].materials[0].com_fc` | ☑ warning |

## 9. 明确不做什么

```
- 不校验 GRS 编号的格式（合规部说编号规则还在变）
- 不复用 #4 的 isRepComp —— 那个判断的是「再生版和普通版并存」，
  和「再生占比超阈值」是两回事，不要合并
- 不改写 com 文档，本 step 只读不写
- 不做 composition 相关校验（平台管）
```

## 10. 数据库

| 项 | 内容 |
|---|---|
| 表名 | `"EV_GRS_CERT"` |
| 查询 key | `"Vendor"` |
| 用到的列 | `"Vendor"`, `"CertNo"` |
| 查不到怎么办 | ☑ warning（见 §8） |
| 能否批量预取 | ☑ 能 —— `vendor` 是 line 级的，整个 Item 只有一个值，放 `setup()` 查一次 |

## 11. 待确认

```
- 阈值 50 是含税前还是含税后的占比？目前按 com_pc 原值算，未做归一化。
- 多个部件都超阈值时，编号只印一个（同一个 vendor），确认对吗？
- grs_cert 在 EDI 输出里要不要也带？目前只进 outputs。
```

---

## 这个范例里，哪几处是防 AI 犯错的

对照上一轮讨论的失效模式，看这份 spec 分别拦在哪：

| AI 的典型错误 | 这份 spec 哪里拦住 |
|---|---|
| 猜字段名大小写 | §3 逐个列出，且提醒了本 format 的混用情况 |
| 把 `≥` 写成 `>` | §7 例子② 专门用「刚好 50」钉死 |
| 把本规则和 `isRepComp` 混为一谈 | §7 例子③ 明说两者独立 + §9 再强调一次 |
| 查不到时抛异常 | §7 例子④ + §8 明确是 warning |
| 顺手改写 `com` | §9 明说只读不写 |
| 自己决定阈值放哪 | §6 R7 指定做成常量 |
| per-row 查表 | §10 明确可批量预取 |
| 编造未定义行为 | §11 把不确定的写出来，附加要求里让它列假设 |

**填这份 spec 花的时间，基本等于你把需求想清楚的时间。** 想不清楚的地方就是 §11，
而 §11 写出来比留空好 —— AI 会针对它提问，而不是替你决定。
